
  import React from 'react';
  import { render } from '@testing-library/react';
  import CreateProposal from './CreateProposal';

  describe('<CreateProposal/> spec', () => {
   
    test('should render <CreateProposal/>', () => {
      render(<CreateProposal/>);
    });

  });

  